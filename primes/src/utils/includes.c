#include "utils.h"


extern inline bool isOdd(size_t number);

extern inline bool isEven(size_t number);

extern inline size_t num2idx(size_t number, size_t offset);

extern inline size_t idx2num(size_t idx, size_t offset);
