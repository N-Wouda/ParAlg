#ifndef DEFINES_H
#define DEFINES_H

// These are optional defines, used to determine the sequence of steps to
// consider for the BSP parallel algorithm, *after* computing the prime
// numbers.
#define GOLDBACH

#endif // DEFINES_H
